# insert_number

README of the folder that contains the 0-insert_number.c

This Function inserts a new node in a sorted singly linked list.
It returns either the address of the new node, or NULL if it failed